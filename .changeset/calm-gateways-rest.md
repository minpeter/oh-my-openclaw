---
"oh-my-openclaw": patch
---

Remove proactive wake/back-online acknowledgements after gateway restart in the apex preset. The agent now recovers context silently and waits for explicit user input when no pending work exists.
